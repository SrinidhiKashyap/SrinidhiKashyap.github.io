/**
 * Application entry point.
 *
 * This is the root component imported by `main.tsx`. It bootstraps the
 * client-side router, global providers (scroll-to-hash, cursor trail,
 * site loader), and renders the page-level routes.
 */

export { WebsiteApp as default } from "./app/WebsiteApp";